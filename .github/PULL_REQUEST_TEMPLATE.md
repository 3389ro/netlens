## Summary

(What does this PR change? One or two sentences.)

## Why

(What problem does it solve? Link the issue with `Fixes #N` if applicable.)

## Test evidence

- [ ] Built locally with `build-release.bat` (Visual Studio 2022 + CMake 3.20+)
- [ ] Ran `NetLens.exe --help` — output looks right
- [ ] Manually scanned a real LAN with the changed code — results sane

If you added a CLI flag, paste the new `--help` output here. If you changed the scan engine or risk model, include a `CHANGELOG.md` entry under an `[Unreleased]` section at the top.

## Notes for the reviewer

(Anything non-obvious — performance trade-offs, design alternatives you considered, edge cases the diff doesn't cover.)

## Licence

By submitting this PR I confirm my contribution is licensed under the MIT License (same as the project) and I have the right to license it that way.
